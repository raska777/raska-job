// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com'], // <== bu qator muammoni hal qiladi
  },
};

module.exports = nextConfig;
